apiVersion: apps/v1
kind: Deployment
metadata:
  name: fiscal
  namespace: production
  labels:
    app: fiscal
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 50%
      maxSurge: 50%
  selector:
    matchLabels:
      app: fiscal
  template:
    metadata:
      namespace: production
      labels:
        app: fiscal
    spec:
      containers:
      - name: fiscal
        image: registry.gitlab.com/pshenmic/ivend/fiscal:1.0.0
        imagePullPolicy: Always
        ports:
          - containerPort: 3500
        readinessProbe:
          httpGet:
            path: /api/v1/status
            port: 3500
          initialDelaySeconds: 30
          periodSeconds: 5
          successThreshold: 1
          failureThreshold: 3
        envFrom:
          - configMapRef:
              name: fiscal-config
      imagePullSecrets:
      - name: gitlabregistrycred
